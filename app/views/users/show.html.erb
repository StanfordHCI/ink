<h1><%= @cur_user.first_name %> <%= @cur_user.last_name %>'s Site</h1>
<% if !((@cur_user.page).nil?) %>
  <%= @cur_user.page.site_name %><br/>
  <%= Tag.find(:all) %><br/>
  <% for panel in @cur_user.page.panels %>
    <%= panel.panel_name %><br/>
  <% end %>
<% end %>

